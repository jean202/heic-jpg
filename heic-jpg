#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"$ROOT_DIR/scripts/build.sh" >/dev/null

exec java -cp "$ROOT_DIR/build/classes" io.github.jean202.heicjpg.Main "$@"
